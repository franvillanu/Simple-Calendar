# Simple Calendar

Static single-page calendar with localStorage persistence and export (PNG/PDF).

## Cloudflare Pages

Deploy as a static site.

Build settings:
- Framework preset: None
- Build command: (leave empty)
- Build output directory: `./`

The entry point is `index.html`.
